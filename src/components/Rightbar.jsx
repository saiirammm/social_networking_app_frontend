import { Box } from "@mui/material";
import React from "react";

export default function RightBar(props){
    return (
        <Box  bgcolor='' flex={2} p={2} sx={{display:{xs: 'none', md: 'block'}}}>
        </Box>
    )
}
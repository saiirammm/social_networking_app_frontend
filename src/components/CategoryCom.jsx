import React, {useState} from 'react'
import { Box } from "@mui/material";

export default function CategoryCom(props){
    return (
        <Box flex={4} p={2}>
            communities filtered by categories
        </Box>
    )
}